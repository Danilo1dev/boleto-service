services:
  auth-service:
    build: ./
    container_name: boleto-service
    restart: always
    ports:
      - "8081:8081"
    environment:
      - SERVER_PORT=8081
    networks:
      - microservices-network

networks:
  microservices-network:
    driver: bridge